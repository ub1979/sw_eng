---
description: Record a cross-session learning — pattern, pitfall, decision, or convention into .sdlc/learnings.jsonl
allowed-tools: [Read, Write, Edit, Glob, Grep, Bash]
---

Follow the `sdlc:learn` skill instructions exactly. The user invoked this command with: $ARGUMENTS
