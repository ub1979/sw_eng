---
description: Run performance benchmark — Core Web Vitals, API latency, bundle size, build time with regression detection
allowed-tools: [Agent, Read, Write, Edit, Glob, Grep, Bash, WebFetch]
---

Follow the `sdlc:benchmark` skill instructions exactly. The user invoked this command with: $ARGUMENTS
