---
description: Run code health dashboard — linter, types, tests, dead code, dependencies → composite 0-10 score
allowed-tools: [Agent, Read, Write, Edit, Glob, Grep, Bash]
---

Follow the `sdlc:health` skill instructions exactly. The user invoked this command with: $ARGUMENTS
